[
  {
    "mutation": ">= → >",
    "snippet": "count: Number.isInteger(it.count) && it.count >= 0 ? it.count : 0,",
    "reason": "boundary-equivalent by value: at it.count === 0 the true branch keeps 0 and the false branch falls back to 0 — the entry's count is 0 either way, so no input distinguishes >= from > here. Negative and non-integer counts are pinned to 0 by tests; positive counts pass through."
  },
  {
    "mutation": "< → <=",
    "snippet": "for (let t = 16; t < 64; t++) {",
    "reason": "Genuinely equivalent: the mutated bound runs one extra schedule iteration that writes w[64], out of bounds on the fixed Uint32Array(64) — typed-array OOB writes are silently discarded, the loop body has no other side effect, and nothing reads past w[63]. Behaviour is bit-identical; the FIPS vectors pass either way. Same vendored sha256 as every sibling repo, same argument (cascade.mjs)."
  },
  {
    "mutation": "< → <=",
    "snippet": "for (let i = start; i < output.length; i++) {",
    "reason": "Genuinely equivalent: the mutated bound runs one extra iteration reading output[output.length], which is undefined — undefined matches none of the branch characters (backslash, quote, braces), no state changes, and the loop then exits exactly as before. Behaviour is identical for every input. Ported verbatim from fallnode/kernel.mjs's shouldEscalate(), which baselined this exact line with this exact reasoning (cascade.mjs)."
  },
  {
    "mutation": "< → <=",
    "snippet": "if (end < start) return { ok: false, why: 'no JSON object in the answer' };",
    "reason": "Genuinely equivalent, checked empirically not just argued: start is always the index of a literal '{' character and end the index of a literal '}' character (indexOf/lastIndexOf results), and one string index can never hold two different characters at once — so start === end can never occur, which is the only case '<' and '<=' would disagree on. Probed a battery of adjacent/nested/reversed-brace inputs in node directly to confirm no input reaches the equal case (triage.mjs)."
  },
  {
    "mutation": "|| → &&",
    "snippet": "if (!isObj(item) || !isStr(item.hash) || !isStr(item.prevHash) || !isObj(item.entry))",
    "reason": "Genuinely equivalent (the middle || variants, both remaining junctions): confirmed via claudedidy/precedent.mjs against 5 existing exact matches (fallnode, agent-proof, fallrouter, meshos, post-proof — the identical vendored ledger guard) before citing, then re-verified the shape holds in THIS file specifically, not just cited blind. A non-string item.hash can never strictly equal the recomputed primitive-string h.hash at the line below (caught there regardless). A non-string item.prevHash can never strictly equal expectedPrev, which is always GENESIS or a prior link's primitive-string hash (caught at the same check regardless). A non-object item.entry is independently refused by verifyByKind's own delegate (verifyCascadeReceipt/verifyTriageReceipt), both of which open with their own isObj(r) guard — checked directly in cascade.mjs line 172, not assumed. The one killable clause (item itself not an object — a hostile array carrying otherwise-honest seq/hash/prevHash/entry, which would sail through every downstream check unless THIS guard catches it) is killed by the suite."
  },
  {
    "mutation": "|| → &&",
    "snippet": "if (r.resolved !== true || typeof r.category !== 'string' || r.category.length === 0) { un",
    "reason": "Genuinely equivalent, both remaining junctions (signal.mjs line 44) — a stronger guarantee than a downstream shadow: escalationSignal only ever reaches this loop over receipts whose whole chain already PASSED verifyLedgerChain, which calls verifyTriageReceipt on every triage entry. Read verifyTriageReceipt directly (triage.mjs lines 136-145) before baselining rather than assuming: it enforces resolved===true if-and-only-if category isStr(...) AND category is one of CATEGORIES (all non-empty strings) — an unresolved receipt is required to carry category===null exactly, nothing else. So for any receipt that can legitimately reach this line, clause 1 (resolved!==true) and clause 2 (category not a string) are PROVABLY equal in value on every input, and whenever clause 2 is false clause 3 (category.length===0) is provably also false, since a passing category is always drawn from the non-empty CATEGORIES list. No hand-built chain can bypass this: a receipt violating the correlation fails verifyTriageReceipt, which makes verifyLedgerChain return valid:false, which makes escalationSignal refuse the WHOLE computation before this loop ever runs (see its own 'refused to signal off a ledger that does not verify' path, tested directly). The && vs || choice between these three clauses is unobservable by construction, not merely untested."
  }
]
